public class WorldMapGeneration {

    private static void generateWorld(){//todo need to see what to return, so I need to decide how to store the world data, maybe make a overworld locations class that holds Location arrays (like towns or mazes)
                                        //then when you enter the location it pulls its location array and I can have a command to switch between overworld and local actions.
        //todo generate random towns with paths and different locations (forests, rivers, mountains, ruins) between them
    }
}
